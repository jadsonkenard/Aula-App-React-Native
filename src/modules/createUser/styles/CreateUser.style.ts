import styled from "styled-components/native";
import { theme } from "../../../shared/themes/themes";

export const CreateUserContainerUser = styled.ScrollView`
    background-color: ${theme.color.neutralTheme.white};

    padding: 16px;
`
